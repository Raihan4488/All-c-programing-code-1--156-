#include <stdio.h>

int main() {
    int decimal;

    printf("Enter a decimal number: ");
    scanf("%d", &decimal);

    // Print as octal using %o
    printf("Decimal %d as octal: %o\n", decimal, decimal);

    return 0;
}
