#include <stdio.h>

int main() {
    long num;
    char str[50];

    printf("Enter a long integer: ");
    scanf("%ld", &num);

    // Convert long to string
    sprintf(str, "%ld", num);

    printf("Long as string: %s\n", str);

    return 0;
}
