#include <stdio.h>

int main() {
    float a, b, result;

    printf("Enter first floating-point number: ");
    scanf("%f", &a);

    printf("Enter second floating-point number: ");
    scanf("%f", &b);

    result = a * b;

    printf("Result = %.2f\n", result);

    return 0;
}
