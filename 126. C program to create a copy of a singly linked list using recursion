#include <stdio.h>
#include <stdlib.h>

// Define node structure
struct Node {
    int data;
    struct Node *next;
};

// Function to create a new node
struct Node* createNode(int data) {
    struct Node *newNode = (struct Node*)malloc(sizeof(struct Node));
    newNode->data = data;
    newNode->next = NULL;
    return newNode;
}

// Function to print linked list
void printList(struct Node *head) {
    while (head != NULL) {
        printf("%d -> ", head->data);
        head = head->next;
    }
    printf("NULL\n");
}

// Recursive function to copy linked list
struct Node* copyList(struct Node *head) {
    if (head == NULL) {
        return NULL; // Base case: end of list
    }

    struct Node *newNode = createNode(head->data);
    newNode->next = copyList(head->next); // Recursively copy the rest
    return newNode;
}

int main() {
    // Create original linked list: 1->2->3->NULL
    struct Node *head = createNode(1);
    head->next = createNode(2);
    head->next->next = createNode(3);

    printf("Original linked list:\n");
    printList(head);

    // Copy linked list using recursion
    struct Node *copiedHead = copyList(head);
    printf("Copied linked list:\n");
    printList(copiedHead);

    return 0;
}
